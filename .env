# ==========================================
# Auto Docker Updater - Configuration File
#
# Instructions:
# Create a file named '.env' in the same folder as updater.sh.
# Adjust the paths below to match your specific server environment.
# ==========================================

# The master directory where all your separate container folders are located.
# The script will automatically scan all subdirectories here.
BASE_DIR="${BASE_DIR:-$HOME/docker}"

# Where to save the update logs (defaults to $BASE_DIR/container-updater/updater.log)
LOG_FILE="${LOG_FILE:-$BASE_DIR/container-updater/updater.log}"

# Directories to exclude from scanning (colon-separated folder names)
# Example: EXCLUDE_DIRS=container-updater:backups:testing
# EXCLUDE_DIRS="${EXCLUDE_DIRS:-}"

# Override the path to the docker binary (optional)
# By default, the script will automatically detect the 'docker' command.
# DOCKER_BIN="${DOCKER_BIN:-}"

# Enable dry-run mode to test without making changes (set to "true" to enable)
# DRY_RUN="${DRY_RUN:-false}"

# Print log messages to console in addition to the log file
# VERBOSE="${VERBOSE:-false}"

# Automatically prune unused Docker images after updating (set to "false" to disable)
# PRUNE_IMAGES="${PRUNE_IMAGES:-true}"

# Autostart exited containers with 'always' or 'unless-stopped' restart policy
# AUTOSTART="${AUTOSTART:-true}"
# AUTOSTART_RETRY_DELAY="${AUTOSTART_RETRY_DELAY:-10}"

# Webhook URL to notify on container update failures (Slack, Discord, Mattermost, Teams, etc.)
# NOTIFY_FAILURE_WEBHOOK="${NOTIFY_FAILURE_WEBHOOK:-}"

# Webhook URL to notify on successful updates / heartbeat summary (optional)
# NOTIFY_SUCCESS_WEBHOOK="${NOTIFY_SUCCESS_WEBHOOK:-}"

# Healthcheck wait timeout in seconds for 'docker compose up --wait' (0 = default timeout)
# COMPOSE_WAIT_TIMEOUT="${COMPOSE_WAIT_TIMEOUT:-0}"

# Enable or disable stack pre-update.sh and post-update.sh hooks (set to "false" to disable)
# RUN_HOOKS="${RUN_HOOKS:-true}"

# Path to the lock file (prevents overlapping cron runs)
# LOCK_FILE="${LOCK_FILE:-}"

# Image pull retry settings (useful for large images or rate limits)
PULL_RETRIES="5"
PULL_RETRY_DELAY="30"

# Log rotation: max log file size in KB before rotating (0 = disabled, 5120 = 5MB)
LOG_MAX_SIZE_KB="5120"


